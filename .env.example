# Spotify API Credentials
# Get your credentials at: https://developer.spotify.com/dashboard

SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# Last.fm API key — used by src/popularity_enrich.py to rank search results by
# artist prominence (listeners), keyed by MBID. Free, no OAuth:
# https://www.last.fm/api/account/create
# Optional: without it, enrichment falls back to graph-degree (coarser but still
# fixes ranking).
LASTFM_API_KEY=your_lastfm_api_key_here
